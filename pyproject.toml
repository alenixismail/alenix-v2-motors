[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "alenix-v2-motors"
version = "0.1.0"
description = "V2 Motors - TC, RMR, SHORT engines for Alenix Personal Trader"
readme = "README.md"
requires-python = ">=3.9"
